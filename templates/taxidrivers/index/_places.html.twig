<section class="section places">
    <div class="section__title">
        <h2>{% trans %}index.place.title{% endtrans %}</h2>
    </div>
    <div class="section__content places__content">
        {% for i in 1..3 %}
            <div class="places__content__topic">
                <div class="places__content__topic__title">
                    <h3>History</h3>
                </div>
                <div class="places__content__topic__places">
                    {{ include('taxidrivers/global/_placeCard.html.twig') }}
                    {{ include('taxidrivers/global/_placeCard.html.twig') }}
                    {{ include('taxidrivers/global/_placeCard.html.twig') }}
                </div>
            </div>
        {% endfor %}

        <div class="places__content__more">
            <h3>Did you not found what are you looking for</h3>
            <a class="btn" href="">Show me more</a>
            <a class="btn" href="">Custom travel</a>
        </div>
    </div>

</section>